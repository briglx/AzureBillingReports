sudo: false

matrix:
  fast_finish: true
  include:
    - python: "3.6"
      env: TOXENV=lint
    - python: "3.6"
      env: TOXENV=pylint

branches:
  only:
    - master

install: 
  pip install -U tox
  pip install -r requirements_test.txt
language: python

script: tox
